---
const schedule = [
  {
    time: "18h30",
    title: "Recepção & Welcome Wine",
    description: "Chegada dos convidados, vinho de boas-vindas e networking inicial entre os 50 empresários selecionados.",
    icon: `<svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M8 2h8l-1 8a5 5 0 0 1-10 0L8 2z"/><path d="M12 15v5"/><path d="M8 20h8"/></svg>`,
  },
  {
    time: "19h00",
    title: "Fast Talks & Entradas",
    description: "Início das palestras rápidas e de alto impacto, integradas ao serviço de entradas gastronômicas.",
    icon: `<svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4L16.5 3.5z"/></svg>`,
  },
  {
    time: "20h30",
    title: "Jantar & Conexões Estratégicas",
    description: "Jantar premium, trocas estratégicas e conexões profundas entre os empresários presentes.",
    icon: `<svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M3 2v7c0 1.1.9 2 2 2h4a2 2 0 0 0 2-2V2"/><path d="M7 2v20"/><path d="M21 15V2v0a5 5 0 0 0-5 5v6c0 1.1.9 2 2 2h3zm0 0v7"/></svg>`,
  },
  {
    time: "22h00",
    title: "Encerramento",
    description: "Fechamento da experiência com os últimos momentos de networking e despedida.",
    icon: `<svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><polyline points="12 6 12 12 16 14"/></svg>`,
  },
];
---

<section id="programacao" class="py-20 md:py-28 px-6">
  <div class="max-w-4xl mx-auto">
    <!-- Section header -->
    <div class="text-center mb-16" data-animate>
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-heading font-bold mb-4">
        <span class="text-gold">Programação</span> do Evento
      </h2>
      <div class="gold-divider"></div>
      <p class="text-white/60 text-lg max-w-xl mx-auto">
        Uma noite cuidadosamente planejada para maximizar conexões e insights.
      </p>
    </div>

    <!-- Timeline -->
    <div class="relative">
      <!-- Vertical gold line -->
      <div
        class="absolute left-6 md:left-1/2 top-0 bottom-0 w-px bg-gradient-to-b from-gold/0 via-gold/40 to-gold/0 md:-translate-x-px"
        aria-hidden="true"
      ></div>

      <!-- Timeline entries -->
      <div class="space-y-12">
        {schedule.map((item, index) => (
          <div
            class={`relative flex items-start gap-6 md:gap-12 ${
              index % 2 === 0 ? "md:flex-row" : "md:flex-row-reverse"
            }`}
            data-animate={index % 2 === 0 ? "fade-left" : "fade-right"}
            style={`transition-delay: ${index * 0.15}s;`}
          >
            {/* Content card - mobile: always right; desktop: alternating */}
            <div class={`flex-1 ${index % 2 === 0 ? "md:text-right" : "md:text-left"} pl-16 md:pl-0`}>
              <div class="bg-navy-light/30 border border-white/5 rounded-xl p-6 hover:border-gold/20 transition-colors duration-300">
                <span class="text-gold font-heading font-bold text-2xl block mb-2">
                  {item.time}
                </span>
                <h3 class="text-white font-heading font-semibold text-lg mb-2">
                  {item.title}
                </h3>
                <p class="text-white/60 text-sm leading-relaxed">
                  {item.description}
                </p>
              </div>
            </div>

            {/* Center dot */}
            <div
              class="absolute left-6 md:left-1/2 w-12 h-12 -translate-x-1/2 bg-navy border-2 border-gold rounded-full flex items-center justify-center z-10 text-gold"
              aria-hidden="true"
            >
              <Fragment set:html={item.icon} />
            </div>

            {/* Spacer for alternating layout on desktop */}
            <div class="hidden md:block flex-1"></div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
